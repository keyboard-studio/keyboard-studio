{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "outDir": "dist",
    "rootDir": ".",
    "composite": true,
    "tsBuildInfoFile": "./dist/.tsbuildinfo",
    "types": ["node"]
  },
  "include": [
    "*.ts",
    "sources/*.ts",
    "test/*.ts"
  ],
  "exclude": ["dist", "node_modules"]
}
