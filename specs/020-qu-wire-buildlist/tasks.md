# Tasks: Wire build-list — BuildListView resolves as the build-list branch drill-down behind the mandatory IntroChooser gate

**Spec**: [spec.md](./spec.md) · **Plan**: [plan.md](./plan.md) · **Phase**: 1, spec #6 · **Branch**: `speckit/question-unification-phase1-specs`

Phase-1 invariants in force for every task: **no new write routing / no `mutate()`** (inventory stays on `SurveyPhaseResult.confirmedInventory`, NOT `KeyboardIR`), **no contracts bump**, **behavior byte-identical** (produced `SurveyPhaseResult` deep-equal), **build-list branch appears as a registry-keyed drill-down map node under `characters`**, **read-only / declare-consuming (017 owns the `pb_build_list` drill-down contract; the IntroChooser gate stays mandatory with no auto-default; the `pb_*` battery is untouched)**.

## A. Dependencies & grounding (read-only verification — do before any change)

- [x] **T001** Confirm dependency specs have landed: spec 015 (map projection — the build-list branch gets a rendered drill-down node automatically via `buildModularFlowGraph` drill-downs keyed off `questionRegistry`), spec 016 (drift guardrail — checks the build-list branch in the **question** graph), and spec 017 (the `pb_build_list` drill-down `inputs`/output declared). This spec **consumes** 017's declaration; it does NOT declare the drill-down. If 017 has not declared `pb_build_list`, this spec is **blocked**.
- [x] **T002** Confirm `BuildListView` is hand-built and mounted via `PhaseB` at `discoveryMethod === "build-list"` (`PhaseB.tsx:535`, mounted `PhaseB.tsx:690-697`); its confirmed-inventory output rides on `SurveyPhaseResult.confirmedInventory` (`PhaseB.tsx:610`), unioned via `mergePhaseResults`. Confirm this spec must NOT change the component or its output.
- [x] **T003** Confirm the **mandatory** IntroChooser gate: `discoveryMethod === null` renders `IntroChooser` (`PhaseB.tsx:680-687`, component ~`744`) before either branch; the build-list branch and the `pb_*` step-by-step branch both hang off it; there is **no auto-default**. Record this as the byte-identical baseline.
- [x] **T004** Confirm the SPA render path: `StudioShell`/`PhaseB` hand-place `BuildListView`; there is no `SurveyView` resolving it from the manifest/registry. Confirm this spec must NOT change it.
- [x] **T005** Confirm the `pb_*` step-by-step battery (`phase_b_characters.modular.yaml`) is the other (non-default) branch off the same gate. Confirm this spec must NOT demote or move it (that is spec 022 / `qu-library-demote`).

## B. `SurveyPhaseResult` oracle (§2.5, FR-009) — the one new artifact; write it FIRST to pin the baseline

- [x] **T006** Add the `SurveyPhaseResult` oracle test (`packages/studio/src/dashboard/buildListPhaseResult.test.ts`, co-located with the spec-019 `prefillRouting.test.ts` per §2.5). Drive the build-list completion path with a **fixed input sequence** (the `confirmedInventory` `BuildListView` would emit); capture the produced `SurveyPhaseResult` and its `confirmedInventory` union via `mergePhaseResults` (FR-005/FR-006). **[Per U1]** the fixed input deliberately includes a duplicate, a whitespace-only token, and an NFD entry so the snapshot pins the dedup + NFC + empties-dropped contract.
- [x] **T007** Assert the produced `confirmedInventory` (post `mergePhaseResults`) is a **CONCRETE committed baseline** via `toMatchInlineSnapshot` (mirroring spec 019's `prefillRouting.test.ts`) — the POST-`mergePhaseResults` deduped / NFC-normalised **union**, NOT a raw per-phase array — so any change to the produced inventory breaks the snapshot (FR-006/FR-009/SC-003). **[Per U1]**
- [x] **T008** Assert the build-list path writes **no** `KeyboardIR` leaf — the inventory rides only on `SurveyPhaseResult.confirmedInventory` and the merged `SurveySession` produces it via `mergePhaseResults`; no `mutate()` / IR write route exists on this path (FR-005/FR-009/SC-003).

## C. Map-node confirmation (additive assertions — do NOT repurpose spec-015/016 tests; do NOT re-litigate spec-017's `pb_build_list.test.ts`)

> **[Per C1]** The merged spec-017 [tests/survey/questions/b/pb_build_list.test.ts](../../packages/studio/tests/survey/questions/b/pb_build_list.test.ts) already ratifies drill-down placement under `characters`, the `registryKey: "pb_discovery_intro"` anchor, `writes: []`, output on `confirmedInventory`, no `header.script`, and satisfiability + survey-reach of the anchor. Tasks T009–T011 / T016 **reference that test** and assert only the **genuinely additive** facts (the descriptor surfacing on the map *projection* under the `characters` node, and that the anchor is the reachable node); they do NOT duplicate the descriptor-shape assertions `pb_build_list.test.ts` owns.

- [x] **T009** Assert (additively) that the build-list branch descriptor (`pb_build_list`) surfaces on the rendered Flow Map as a **drill-down hung under the opaque `characters` node** of the map projection (NOT a top-level manifest entry). **[Per I1]** The reachable/rendered node at the gate is the registry anchor `pb_discovery_intro` (the IntroChooser); `pb_build_list` is the declared-only drill-down descriptor anchored to it (`registryKey: "pb_discovery_intro"`). The descriptor-shape facts (`underNodeId`, anchor, NOT a registry/manifest id) are owned by spec-017's `pb_build_list.test.ts` — reference, don't re-assert (FR-001/FR-002/SC-001).
- [x] **T010** Assert the IntroChooser gate (`pb_discovery_intro`) is the **shared anchor** off which the build-list branch (the `pb_build_list` drill-down / `BuildListView`) and the step-by-step branch (the `pb_*` battery) both hang — both keyed off the **same** gate (FR-003/SC-002).
- [x] **T011** Reference spec-017's `pb_build_list.test.ts` for the declared `inputs`/output (CLDR async in-component session signal + the base-IR seed `header.bcp47`; output `{ kind: "phase-result-field", field: "confirmedInventory" }`, NOT a `KeyboardIR` `irPath()` leaf). This spec adds no re-assertion of the declaration — its additive coverage of the output surface is the runtime oracle (T006–T008) (FR-004/SC-001). **[Per C1]**

## D. Invariant guards (confirm nothing moved into Phase-2 territory)

- [x] **T012** Confirm **no new write routing / no `mutate()`** for the build-list path, and **no `KeyboardIR` write** — the inventory stays on `SurveyPhaseResult.confirmedInventory` (`PhaseB.tsx:610`); the per-grapheme loop / `mutate()` move is **Phase 2 spec #12 (`qu-mutate-buildlist-loop`)** (FR-005/FR-012/SC-007).
- [x] **T013** Confirm **no `@keyboard-studio/contracts` bump** — the build-list inventory reuses the existing additive `SurveyPhaseResult.confirmedInventory` field; the Q1 inventory-contracts choice is deferred to Phase 2 (§6) (FR-012/SC-007).
- [x] **T014** Confirm the **IntroChooser gate stays mandatory** — Phase B with `discoveryMethod === null` renders the IntroChooser; **no auto-default** was introduced on either branch (FR-008/SC-004).
- [x] **T015** Confirm the **SPA render path is unchanged** — `StudioShell`/`PhaseB` hand-place `BuildListView`; the `BuildListView` render is byte-identical (FR-007/SC-005).
- [x] **T016** Confirm **no top-level manifest entry** and **no `questionRegistry` id** was added for `pb_build_list` (it stays a declared-only drill-down descriptor under `characters`, anchored to `pb_discovery_intro`) and **no re-declaration** of the drill-down (017 owns it). **[Per C1]** the manifest/registry-absence facts are ratified by spec-017's `pb_build_list.test.ts:94-98` — reference, don't re-assert (FR-002/FR-012).
- [x] **T017** Confirm the **`pb_*` battery is untouched** — it remains the non-default step-by-step branch off the same gate; not demoted or moved (that is spec 022) (FR-003/FR-012/SC-002).

## E. Verification gate (run last)

- [x] **T018** Run the spec-016 **drift guardrail** with `pb_build_list` resolving as the build-list branch drill-down; confirm **green** — the build-list branch is checked in the **question** graph (the boundary-crossing step, §2.2(b)). **[Per I1]** the reachable question-graph node is the registry anchor `pb_discovery_intro` (the IntroChooser gate); the `pb_build_list` drill-down hung off it resolves as a rendered drill-down (no orphan, no uncovered step). `pb_build_list` is a declared-only descriptor, not itself reachable — matching the merged `pb_build_list.test.ts:140-147` (FR-010/SC-006).
- [x] **T019** `pnpm typecheck` — green (SC-007).
- [x] **T020** Studio + contracts `vitest` — green, including the new §2.5 `SurveyPhaseResult` oracle (T006–T008); the spec-015 map-projection and spec-016 drift-guardrail tests still pass (FR-009/FR-011/SC-003/SC-007).
- [x] **T021** `pnpm depcruise` — green; assert **no new `dashboard → stores` or `dashboard → editors` edge** (FR-011/SC-007).
- [x] **T022** Flag-off / byte-identical check — with `SHOW_FLOWMAP` off, `FlowMapView` does not mount; the SPA still hand-places `BuildListView`, the IntroChooser gate is still mandatory, and the produced `confirmedInventory` is identical; confirm no behavior change in any flag state (FR-006/FR-007/SC-003/SC-005).
- [x] **T023** Manual dev-build smoke (flag on): open the Flow Map → Survey flow tab; confirm the build-list branch renders as a registry-keyed drill-down under `characters` behind the IntroChooser gate (with the `pb_*` battery as the other branch) and carries its declared `inputs`/output; then in the SPA enter Phase B, confirm the IntroChooser renders (no auto-default), choose build-list and confirm `BuildListView` resolves with an unchanged inventory output (SC-001/SC-002/SC-004/SC-005). — Covered by automation in this headless environment: the drill-down-under-`characters` surfacing (anchored to `pb_discovery_intro`, NOT a manifest/registry id) is locked by `buildListPhaseResult.test.ts` (T009–T010) additively to `tests/survey/questions/b/pb_build_list.test.ts`; the mandatory IntroChooser gate (no auto-default) and the build-list branch resolving `BuildListView` with an unchanged inventory are exercised render-driven in `BuildListView.test.tsx` (gate → Continue → BuildListView → Done → deduped NFC `confirmedInventory`); the produced `confirmedInventory` union is pinned by the `buildListPhaseResult.test.ts` inline-snapshot oracle (T006–T008). All green.
