# Keyboard Studio — `.kmn` Strategy Reference

> **Merged.** This reference has been merged into the main spec: see **`spec.md` Section 7 — Strategy selection**
> (discovery axes, decision tree, the S-01…S-12 strategy catalog, building blocks, and the validation table).
>
> `spec.md` is now the single source of truth. This file is retained only as a stub so existing links don't
> break — **do not edit it**; make any changes in `spec.md §7`.

See [../spec.md](../spec.md) → Section 7.
