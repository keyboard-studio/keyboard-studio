// Phase 1 (Setup) placeholder — the catalog + relatedness + bridge surface
// (getLanguoid, byIso639p3, ancestors, relatedLanguages, relatedIsoCodes,
// findKeyboardBaseCandidates) lands in later phases of specs/036-glottolog-catalog.
// This file exists only so the empty package skeleton compiles and the
// workspace can resolve `@keyboard-studio/glottolog`.
export {};
