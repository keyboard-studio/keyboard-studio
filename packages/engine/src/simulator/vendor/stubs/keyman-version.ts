// Stub for @keymanapp/keyman-version used by the vendored web-utils/version.ts.
// The simulator does not use the version for any keyboard-rule-evaluation path;
// we supply a fixed value so the Version class can construct without error.
const KEYMAN_VERSION = {
  VERSION_RELEASE: "19.0.0",
};
export default KEYMAN_VERSION;
