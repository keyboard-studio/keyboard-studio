# Keyboard Studio Spec — Review Sign-Off

**Spec version:** 1.3.1 (original sign-off: 1.0, 2026-06-02)
**Date:** 2026-06-15
**Spec file:** keyboard-studio-spec-draft.md (1044 lines, 19 sections)
**Reviewer crew:** lex-doc (drafting + revision), lex-qc, lex-domain, lex-synthesis
(lex-verification was not used — this was a doc-authoring cycle; no code or tests to verify.)

---

## Review Cycles

| Cycle | Focus | Reviewers | Outcome |
|-------|-------|-----------|---------|
| 1 | Discovery + initial drafting | Explore agent (digest), lex-doc (draft) | 519-line draft; 5 inline OPEN items |
| 2 | Full parallel review | lex-domain, lex-qc, lex-doc (second-pass structural critique) | 5 P0, 12+ P1, ~10 P2 issues identified |
| 3 | Revision pass | lex-doc | 393 lines; all P0+P1 closed; ToC restructured 14->18 sections; OPEN items resolved as DECISIONS; Glossary and Revision policy added |
| 4 | Targeted spot-check on revision deltas | lex-qc, lex-domain (parallel) | 9 PASS + 1 cosmetic PARTIAL (qc); 8/8 PASS (domain); one P2 footnote opportunity noted |
| 5 | P2 micro-edit + sign-off | lex-doc (footnote), lex-synthesis (this document) | P2 closed; sign-off issued |

---

## Decisions Baked In (Sec 14)

- **D1 — Partial slot-fill:** Block on required slots; allow optional empty only if substituted fragment passes Layer A validation.
- **D2 — CasedKeys for non-Roman:** Default omit; survey-prompt shown for case-distinct script subtags: Armn, Geor, Adlm, Osge, Wara, Cher.
- **D3 — Lint vs. compile:** One debounce cycle (300 ms), two concurrent microtasks; TS error suppresses WASM call; WASM diagnostic supersedes conflicting TS diagnostic.
- **D4 — Quality bands:** Four bands: scaffolder-bake / Layer-C-enforce / yellow-survey / red-checklist.
- **D5 — CJK/Ethiopic:** Confirmed out of v1; routing renders "not yet supported" stub.
- **D6 — Desktop-first authoring scope:** v1 is desktop-first only; touch layout produced in Phase E from desktop OSK; mobile-primary authors notified at Phase A before survey investment; touch-first authoring is a v1.1 candidate.

---

## Residual Items

**NONE.** All P0, P1, and P2 items flagged across cycles 1-4 were closed in-cycle.

Three items noted in the spec's Revision policy (Sec 18) — Risk/dependencies section, Performance targets table, Accessibility section — are scope expansions tracked for v1.1, not residual review findings from this cycle.

---

## Post-Sign-Off Amendments

- **2026-06-02 (v1.0.1):** Added Decision 6 (desktop-first authoring scope) after pre-implementation review identified the mobile-first phase-order gap as a v1 BLOCKER (lex-domain Cycle 1). Also added §7.1 sub-axis A7a (full-remap detection) and §7.2 rule 8 (alphabetic full-remap → S-06 + S-04 + S-08) to close the Armenian/Cyrillic mis-routing BLOCKER from the same review.
- **2026-06-02 (v1.0.2, Cycle 3 close-out — Day-1 contract lock APPROVED):** LEX crew 3-cycle review (lex-domain, lex-doc, lex-qc, lex-verification) closed APPROVED. Locked contract surface in `packages/contracts`: Pattern (with `strategyId: StrategyId` / `combinesWith: StrategyId[]`), PatternMatch (optional strategyId), DiscoveryAxisVector (camelCase fields per §7.1; A2a + A7a sub-axes), VirtualFS (`serializeZip(): Promise<Uint8Array>` — Node + browser portable), `makePattern()` factory + `PatternInit`, `ALL_STRATEGY_IDS`, `StrategyId` union. Spec §5 amended in v1.0.2 to use StrategyId in the Pattern interface (was `string`). §7.5 Armenian and Russian rows corrected to S-06 + S-04 + S-08 with new S-04 building-block legend note. §7.2 prose updated with rule-11 description. Build status: typecheck/test/build all exit 0; 7/7 tests pass. Two micro-cleanups (this commit's predecessor): spec.md S-08 card "rules 9" → "rule 10" typo; pattern.test.ts non-null assertions on bare ALL_STRATEGY_IDS index access. Parallel engine + content team phase cleared to start per spec §13.
- **2026-06-08 (v1.1.0, KeyboardIR import + functional round-trip):** Added §5a KeyboardIR schema (typed IR for parsed `.kmn` + `.kvks` + `.keyman-touch-layout` files), decisions D7-D9 (functional-equivalence round-trip, opaque imports for unrecognized features, IR-is-canonical), Layer A' import-fidelity checks I1-I5 in §10, and the source-selection-then-carve-gallery flow in §8. Removed the v1 exclusion on editing existing keyboards from §16. Added two optional `Pattern` fields (`origin`, `ownedNodes`) linking lifted patterns back to their owning IR nodes. The full amendment proposal lives at [docs/spec-amendment-2026-06-08-keyboardir.md](spec-amendment-2026-06-08-keyboardir.md). Held: the formal #5b joint engine+content session that ratifies the KeyboardIR schema and Pattern field additions has not yet run — this commit captures the spec language for that session to review. Five open questions are tracked at the bottom of the amendment proposal file.
- **2026-06-11 (v1.1.1, corpus-derived placement priors):** Prose-only amendment from the KM crew placement-intelligence review ([docs/placement-intelligence-review.md](placement-intelligence-review.md)). Added §7.6 (placement priors mined from `release/`: codec post-pass extraction, independence-weighted aggregation, blending order, precedent-vs-first-principles precedence rule), §7.5.1 (corpus-scale evaluation of the decision tree emitting `StrategyDivergence` records), per-strategy **Placement semantics** notes on the S-02/S-05/S-06/S-07/S-09 cards in §7.3, and the Phase B placement-proposal protocol in §8. No `Pattern` schema change; `priorSource`/`priorCount` land on the not-yet-locked placement-map type via the kbgen joint session. Amendment record: [docs/spec-amendment-2026-06-11-placement-priors.md](spec-amendment-2026-06-11-placement-priors.md).

- **2026-06-09 (v1.1.1, #232 KeyboardIR contract lock closed):** KM crew cycle 1 — six parallel ratification audits (km-keyman, km-domain, km-strategy, km-author, km-validator, km-output) all returned APPROVE-WITH-CORRECTIONS. Cycle 2 group 1 — km-programmer applied Tier 1 schema corrections; group 2 — km-domain, km-keyman, km-validator, km-verification all ratified; group 3 — km-doc (this pass). Spec bumped to v1.1.1; `@keyboard-studio/contracts` bumped to 0.3.0 (additive minor, pre-1.0 conventions per §18). Build: `pnpm -C packages/contracts typecheck` exit 0, `pnpm -C packages/contracts test` 163/163 pass, `pnpm -r typecheck` exit 0 across contracts/engine/studio/studio-poc.

  **Tier 1 schema corrections** (km-programmer, ratified by km-domain/km-keyman/km-validator/km-verification): (1) `IRHeader.storeDirectives` typed as `string[]` not `StoreItem[]` (km-keyman); (2) `TouchLayoutIR` restructured to `platforms: Array<{id,font?,layers}>` per real file shape (km-output); (3) `KvksIR` keys typed as `{vkey,label,chars?}` not `{vkey,output}` — label/chars split matches real `.kvks` schema (km-keyman); (4) `TouchLayoutIR.nodeIds` and `KvksIR.nodeIds` changed from `Map<string,IRNodeRef>` to `Array<[string,IRNodeRef]>` for JSON round-trip (km-output); (5) `RoundTripDiff.corpusSpec` added — pins corpus parameters for reproducible divergence reports (km-keyman); (6) `KvksIR.kvksVersion?` and `kbdname?` header fields added (km-keyman); (7) `IRHeader.storeDirectives` JSDoc updated (km-keyman).

  **Tier 2 spec corrections**: (T2-#8) §5a sketch `TouchLayoutIR`/`KvksIR` `nodeIds` corrected to `Array<[string,IRNodeRef]>` with JSON-round-trip note (km-output); (T2-#8b) `storeDirectives` sketch type corrected to `string[]`; (T2-#9) D7 amended with two paragraphs: d=3 runtime justification and I2 concurrency model (km-keyman/km-validator); (T2-#10) §5a gallery ranking rule added for recognizer/tree disagreement case (km-strategy); (T2-#11) Layer A' check I6 (ownership consistency) added to §10 table (km-validator); (T2-#12) §12 VFS tree and prose updated with HISTORY.md "Adapted from" scaffolder behaviour (km-output); (T2-#13) JSDoc on `ContextElement.baselayout.value` added to `keyboard-ir.ts` (km-keyman); (T2-#14) spec version bumped to 1.1.1; (T2-#15) D10-D14 registered in §14.

  **Resolved open questions** (from v1.1.0 amendment proposal):
  - Q1 (-> D10): Recognizer rule format — TypeScript predicates; YAML DSL deferred to v1.2 (#273). (km-strategy)
  - Q2 (-> D11): `.kmn.imported` sidecar removed unconditionally; HISTORY.md bullet + PR body supersede it. (km-output)
  - Q3 (-> D12): I2 depth ratified at 3; runtime ceiling binding, not combinatorics. (km-keyman, km-validator)
  - Q4 (-> D13): RawKmnFragment boundary — five categories ratified for v1.1; scanner-driven additions (#237) land as additive minor bumps. (km-keyman)
  - Q5 (-> D14): Attribution carriers ratified — HISTORY.md mandatory, PR body informational, LICENSE.md only if source licence requires, README.md never. (km-output, km-author)

  **Tier 3 known-gaps** (deferred, not blocking v1.1.1): (a) `RawKmnFragment.sourceFile?` — v1.1.x follow-up (km-output); (b) `OutputElement` JSDoc re `use()` and output-position `context` falling to `raw` per D8 — tracked at #268 (km-keyman); (c) deadkey naming (`dk(N)` vs `dk(acute)`) — pre-existing observation from km-author, Layer B style issue not Layer A correctness, follow-up minor bump; (d) TouchLayoutIR/KvksIR sub-file opaque escape hatch — v1.1.x; v1 codec required to refuse or warn rather than silently drop unmodeled sub-structures (§16 scope).

  **Reviewed by:** km-keyman, km-domain, km-strategy, km-author, km-validator, km-output (cycle 1) + km-programmer (cycle 2 group 1) + km-domain, km-keyman, km-validator, km-verification (cycle 2 group 2) + km-doc (cycle 2 group 3).

- **2026-06-13 (v1.2.0, hybrid authoring workflow + scoped DISCUS-guided gallery):** Prose amendment deriving from the workflow-modeling session captured in [docs/workflow-model.md](workflow-model.md). Rewrote §8 to a **hybrid** authoring order — identity-lite → base resolution (suggest from the phonebook / manual / US-QWERTY blank) → base-derived prefill (routing/A2/A7/BCP47 as confirmations) → inventory (diffed vs base) → physical gallery (Phase C) → **lock desktop** → touch gallery (Phase E) → deferred documentation/metadata — via an ordering preamble over the existing numbered steps (steps relocated, not renumbered). Added §8 "Gallery instantiation" subsection (the gallery is a role instantiated once per modality, with the physical→touch mechanism-mapping table) and §7.7 "Gallery output and assignment-map precedence" (scoped default/class/individual map, individual > class > default, many-to-many mechanisms per character, DISCUS arbitration of the Discoverability-vs-Simplicity tension, coverage = dead-end via criterion 18.6 `KM_LINT_INVENTORY_UNCOVERED`). Reframed the §7 intro and §7.3 catalog (cards are mechanism templates, not whole-keyboard verdicts) and scoped the §7.5 table to default-scope tree validation. Added §5 "Base-derived pre-fill". Clarified §9 that there is no mobile-first routing (`pa_primary_target` advisory; Decision 6). **Decoupled language and script** (§8 identity-lite, §9): the keyboard target is a *(language, script) pair* with script chosen independently — romanization (`-Latn`) and IPA (`-fonipa`) are first-class alternate-script paths; the chosen script (not the language's default) drives routing, A2, base suggestion, and the inventory diff. Spec bumped to v1.2.0. **Held — blocks on the joint engine+content session (#5b):** the *typed* assignment-map contract. Until that session, the contract carries it as an additive optional field (`SurveyPhaseResult.assignments?`) alongside the flat `selectedPatternIds`; the breaking redesign that collapses the two is the joint-session deliverable (proposal: `docs/proposal-assignment-map-contract.md`). No `Pattern` schema change in this amendment.

- **2026-06-14 (v1.3.0, working-copy spine + two authoring tracks):** Prose-only amendment (single-reviewer class per §18). Added the "Two authoring tracks, one working-copy spine" section to §8: documents the persistent working copy (`KeyboardIR` + `VirtualFS` pair) instantiated at keyboard selection, mutated by every step, and serialized only at output; the two entry tracks — Track 1 `instantiateFromBase` (copy + reset identity, enters hybrid survey flow) and Track 2 `instantiateFromExisting` (load + preserve identity, enters via source-picker); convergence on a shared spine after instantiation. Added the OSK-spacebar-caption note (identity edits are visible OSK mutations on the spacebar; script/base/carve/mechanism edits change key glyphs). Added `buildProducedSet` pointer (`packages/contracts/src/ir/producedSet.ts`) to the §8 inventory-diff bullet. Added "Working copy as the live edit target" and "OSK spacebar caption as a visible identity mutation" paragraphs to §12, documenting that assignments + carve deletions are re-projected layers (not destructive IR edits) and that the working copy is serialized only at output. Extends Decision 9 (IR is canonical). No `Pattern` schema change. Does not reopen any resolved decision (D1–D14). **Reviewed by:** km-doc (single-reviewer prose class). **Date:** 2026-06-14.

- **2026-06-15 (v1.3.1, defaults-first principle + §8 defaults audit follow-ups):** Prose-only amendment (single-reviewer class per §18). Added **§3c "Defaults are the product"** — restates the "no default is a defect" principle (naive users accept reasonable defaults; the studio's value is in the defaults, not the override controls; a weak default is a user-visible failure), generalizes the §5 *never ask before you can pre-fill* rule to a universal **propose-then-confirm** posture, names the three zero-flag model patterns (§7.2 input-contract default-fill / `axisFills`, §7.6 corpus priors via *research-by-pairs*, §5 base-derived pre-fill), and mandates provenance labelling + a hinted-prompt no-default floor. Resolved the eight §8 defaults-audit flags by adding explicit propose-then-confirm prose at each decision point, working through each flag interactively with the spec owner (several diverged from the issue's literal suggestion):
  - **Phase A copyright holder** — a *structured choice* (you / an organization), not a blind fill: the *you* branch pre-fills the GitHub OAuth display name (→ provenance representative), the *organization* branch a hinted free-text field; the studio never silently asserts the OAuth identity as the holder, since submitter ≠ copyright holder.
  - **Phase A display name** — the scaffolder's provisional value made explicit as an editable confirmation at the documentation stage.
  - **Phase A autonym** — pre-filled from `langtags.json` `localname` (already loaded for the BCP47 lookup; better minority-language coverage), CLDR secondary, hinted prompt floor (corrects the issue's CLDR-only source).
  - **Phase B Q2** — reframed from "co-installed keyboards" to the *target user community's coexisting keyboards*, driving coverage burden (only-keyboard vs contact-language coexistence) and AZERTY/QWERTY transition expectations; proposal derived from region official/contact languages + Q1. The issue's browser/OS auto-detect was dropped as technically unsound (browsers cannot enumerate installed layouts).
  - **Phase B Q6 primary use case** — axis-aware pre-selection from A1 scale + region/speaker-count provenance + Q1, non-gating.
  - **Phase C′ Non-Roman reorder** — a script-family reorder prior parallel to §7.6 (A2 + §9 sub-routing, ranked, provenance-shown; §7.6 precedence + §16 CJK/Ethiopic exclusion apply).
  - **Phase E touch-layer naming** — modifier-derived layer ids auto-derived from Keyman's standard touch-layout layer-id vocabulary (`default`/`shift`/`numeric`/`symbol`/`rightalt`/`caps`) via the modifier→layer map; only author-added non-modifier planes need a hinted name (corrects the iOS/Android framing).
  - **Phase F help body** — the `welcome.htm` body is not a blank canvas: a deterministic skeleton (title, autonym, character→keystroke table from the Phase B inventory + §7.7 assignment map) is built first; the linguist agent only embellishes the surrounding prose, keeping hallucination out of the shipped how-to-type instructions.

  §3c also gained the corollary that a *confidently wrong default is as much a failure as a blank one* — weak-signal or attribution-sensitive fields get a structured choice or a hint rather than an asserted value. No `Pattern` schema change; does not reopen any resolved decision (D1–D14). All eight flags closed via explicit prose — every flag admitted a defensible default, so no deliberate "no-default" rationales were required. Also reconciled the §3c mandate with the §5 `PatternQuestion.default` optionality (and the matching JSDoc in `packages/contracts/src/pattern.ts`) using the §7.1 `axisFills` model — the optional field is a static-slot vs. runtime-fill split, not licence to ask blank; a derivable-but-blank question is a defect; the typed `defaultSource` hardening is deferred to the #5/#5b joint session. Comment-only contracts touch (no `Pattern` schema field change). Review (km-doc + km-keyman) confirmed the change is non-breaking and the deferral correct, and flagged that the `axisFills` provenance record is itself planned, not yet typed — both §3c and §5 now describe it (and `defaultSource`) in the same deferred register. **Reviewed by:** km-doc, km-keyman (single-reviewer prose class). **Date:** 2026-06-15.

- **2026-06-26 (D9 extension — emit() orphan-store preservation, issues #600 and #601):** Two store-emission gaps in `packages/engine/src/codec/emit.ts` were closed under the D9 (IR-is-canonical / lossless render) guarantee, with no spec-text change required. Both gaps share the same rationale: `emit(ir): string` has no diagnostic channel, so the only correct response to a data-loss risk is preservation, not a warning that would force a signature change and still lose data.

  - **Issue #600 — fragment-free path, unreferenced user stores:** A non-system `store(...)` whose name appeared in no typed rule was silently dropped by the fragment-free emit path. Fixed by an orphan-store pass (see the `emit.ts` declaration-order sweep before the `begin` block) that emits all such stores in their original declaration order.
  - **Issue #601 — faithful path, unsourced stores in global fragments:** A store with no `sourceLine` that was referenced only inside a fragment not reached by any group's per-group check (e.g. a global fragment) escaped the per-group accounting and was dropped. Fixed by a post-group catch-all sweep over unsourced stores not yet emitted (see `emit.ts`).

  Both fixes are minimal-additive: currently-correct keyboards remain byte-identical; only keyboards that previously lost data are affected. No `Pattern` schema change; does not reopen any resolved decision (D1–D14). **Extends D9.** **Date:** 2026-06-26.

- **2026-06-16 (spec-kit section extraction — §5 Pattern schema):** Migration step, not a content change. §5 extracted verbatim into [`specs/005-pattern-schema/spec.md`](../specs/005-pattern-schema/spec.md) (now authoritative for §5); `spec.md` keeps a stub pointer. Third section migrated after §7 (pilot) and §8 (both 2026-06-15, not previously logged here). The extracted file records the authority order explicitly: the canonical `Pattern` type is `packages/contracts/src/pattern.ts` (runtime-enforced by the zod schemas added in `packages/contracts/src/schemas.ts`, with compile-time drift guards), the extracted prose is the Day-1 reference, and field renames/type changes/removals remain a locked-contract change (major `@keyboard-studio/contracts` bump + joint engine+content session, §18). Constitution Principle I repointed to the extracted folder + the schema-in-same-change rule. No `Pattern` schema field change; does not reopen any resolved decision (D1–D14). §5a (KeyboardIR) remains authoritative in `spec.md` pending its own pass. **Date:** 2026-06-16.

- **2026-06-28 (§18 contract change — executable `mutate` seam + `TouchKeyIR` provenance, spec-014):** Ratified during a weekend no-commit window by **Matthew Lee** (contract authority). Locked-surface addition to `packages/contracts`, bumped `@keyboard-studio/contracts` **0.11.0 → 0.12.0** (0ver minor; spec-014 §3.6/FR-011 classes the `TouchKeyIR` edit as a MAJOR contract change, which maps to a minor bump under this package's pre-1.0 0ver discipline — same convention as the #232 lock's 0.3.0 additive minor and the IRPath 0.11.0 breaking minor). **Field:** `TouchKeyIR.provenance?: TouchKeyProvenance` where `TouchKeyProvenance = "base-derived" | "physical-suggested" | "hand-set"` (optional/additive; absent ⇒ `"hand-set"`, never auto-clobbered) — added in `keyboard-ir.ts`, mirrored by `TouchKeyProvenanceSchema` + a drift guard in `schemas.ts`, exported from `index.ts`, and re-exported by `editors/assignLoop/provenance.ts` (single source of truth, FR-008). **Signature:** `QuestionModule.mutate?(value, ctx: MutateContext): Partial<KeyboardIR>` activated at the type level in `packages/studio/src/survey/types.ts` — a pure function returning a patch the reducer applies as a path-scoped deep merge restricted to the module's declared `writes` (out-of-`writes` writes fail fast and reject the whole patch; idempotent on re-apply). **Lineage:** spec-014 (`specs/014-mutate-seam-touch-propagation/`) and the closed **#232** (KeyboardIR schema lock). **Scope:** contract surface only — the reducer apply path (task T014), per-module `mutate` implementations, the global `mutate` flag, and touch re-propagation remain gated P5 front-end work. **Reviewed by:** Matthew Lee (contract authority, §18). **Date:** 2026-06-28.
- **2026-06-28 (§18 contract change — touch layout as a first-class mutable IR surface, spec-014 Phase 5 Cycle A):** Ratified by **Matthew Lee** (contract authority): touch mutation is routed through `KeyboardIR` — **one write surface for both physical and touch outputs** — rather than through a separate touch side-car path. The existing `KeyboardIR.touchLayout?: TouchLayoutIR` field (optional/additive) is the canonical mutable home; per-key origin lives on `TouchKeyIR.provenance` (landed in the 0.12.0 note above). This cycle delivers the contract plumbing that surface needs and bumps `@keyboard-studio/contracts` **0.12.0 → 0.13.0** (0ver minor; additive — no field renamed/removed/retyped, existing IRs stay valid). **Added:** (1) `TouchKeyIRSchema` (self-recursive over `sk`/`flick`/`multitap`; `provenance` optional, absent ⇒ `"hand-set"` on parse per FR-009), `TouchLayoutIRSchema`, and a `KeyboardIRSchema` (touch surface validated precisely, remaining not-yet-schematized top-level IR fields passed through) in `schemas.ts`, each with an Art. I drift guard; exported from `index.ts` via `export * from "./schemas"`. (2) The engine codec now round-trips per-key provenance through a non-standard `"p"` `.keyman-touch-layout` property (`emitKey` writes it, `convertKey` reads it, defaulting absent/legacy/out-of-vocabulary → `"hand-set"`); kmcmplib's touch-layout reader ignores `"p"`, so the addition is non-breaking (FR-010). (3) `TOUCH_WRITES` — the `touchLayout.platforms[].layers[].rows[].keys[]` + `touchLayout.nodeIds[]` `IRPath` containment set — is exported from `packages/studio/src/steps/editorMutate.ts` alongside `CARVE_WRITES`/`ADD_GALLERY_WRITES`, and a containment test confirms a touch-key re-propagation patch passes `applyMutatePatch` while a stray non-touch write is rejected whole (M1–M5). **Lineage:** spec-014 (`specs/014-mutate-seam-touch-propagation/`), US3 tasks **T026–T028**; builds on the 0.12.0 lock above. **Scope:** contract + codec + IR-path foundation only — US2 re-propagation logic (`repropagate.ts`, the `applyStepCompletion` touch trigger, `touchSuggest`'s body, `physical-suggested → hand-set` promotion) remains the next cycle's gated front-end work. **Reviewed by:** Matthew Lee (contract authority, §18). **Date:** 2026-06-28.

---

## Sign-Off

Approved for v1.0 release.

**Reviewed by:** lex-doc, lex-qc, lex-domain, lex-synthesis
**Date:** 2026-06-02
